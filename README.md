# Hand-Written-Digit-Recognition-System
A machine learning project that recognizes handwritten digits using an MLP Classifier trained on the MNIST dataset with data preprocessing, model training, and prediction capabilities.
